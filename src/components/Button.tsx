import { Button as MUIButton} from "@mui/material";

const Button:React.FC = () => {
  return (
    <div className="Button">
      <MUIButton variant="contained">ボタン</MUIButton>
    </div>
  );
}

export default Button;
